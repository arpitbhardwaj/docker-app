apiVersion: v1
kind: Pod
metadata:
  name: dockerapp-pod
  labels:
    app: dockerapp
spec:
  containers:
    - name: docker-app
      image: arpitb/docker-app
      ports:
        - name: nodejs-port
          containerPort: 3000